[build-system]
requires = ["hatchling >= 1.26"]
build-backend = "hatchling.build"

[project]
name = "encryptjson"
version = "1.0.2"
readme = "README.md"
dependencies = [
  "cryptography"
]

[tool.hatch.build.targets.wheel]
packages = ["encrypted_json"]